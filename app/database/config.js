const express = require('express');
// import * as dotenv from "dotenv";
// dotenv.config();


var config =
    {
        user: "postgres",
        host: "127.0.0.1",
        database: "Astradux",
        password: "postgres",
        port: 5432,
    };
module.exports = config;
// export default config;